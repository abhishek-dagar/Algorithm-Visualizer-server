const { Category } = require("./Category");
const { Algorithm } = require("./Algorithm");
const { files } = require("./Files");
module.exports={
    Category:Category,
    Algorithm:Algorithm,
    files:files,
}